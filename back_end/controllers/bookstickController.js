const Film = require('../models/film');
const UpcomingFilm = require('../models/upcomingfilm');
const mongoose = require('mongoose');

const bookstickController = {
}
module.exports = bookstickController;